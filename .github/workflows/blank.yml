name: Hello Python action

on:
  push:
  workflow_dispatch: 

jobs:
  hello:
    runs-on: self-hosted

    steps:

      - name: Run Hello World
        run: |
          python -c "print('Hello, world!')"
